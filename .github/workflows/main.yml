name: Ubuntu VPS #07
on: { workflow_dispatch: {} }

jobs:
  vps07:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Start SSH session (tmate) - #07
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: false
