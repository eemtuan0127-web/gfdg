name: Ubuntu VPS #05
on: { workflow_dispatch: {} }

jobs:
  vps05:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Start SSH session (tmate) - #05
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: false
